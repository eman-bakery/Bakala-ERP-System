# AGENTS.md

## Cursor Cloud specific instructions

### Repository Status

This repository (`Bakala-ERP-System`) is currently an empty project with only a `README.md`. There is no application code, no package manager configuration, no build system, and no tests.

### Development Environment

- No dependencies to install.
- No services to start.
- No build, lint, or test commands are configured.

### Notes for Future Agents

- When application code is added, update this file with the relevant setup, build, lint, test, and run instructions.
- The update script (via `SetupVmEnvironment`) is currently a no-op (`true`) since there are no dependencies to install.
